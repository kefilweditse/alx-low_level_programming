#include "main.h"

/**
 * _isupper - Check Upper letter
 * @i: The number to be checked
 * Return: 1 for upper or 0 for any
 */

int _isupper(int i)

{

	if (i >= 65 && <+ 90)
	{
	return(1);
	}
	return(0);
}
